import Web3 from 'web3';

// Connect to the Ethereum network using a provider
const ethereumConnection = new Web3('https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID');

export default ethereumConnection;
